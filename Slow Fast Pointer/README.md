## Points to remember

# Floyd Cycle detection algo
- used when asked to detect loop, cycle, repetition
- uses a slow and fast pointer to run through the linkedlist
- slow is head and fast is usually fast->next->next
- because of that in while loop to avoid crash fast and fast-> next can't be null